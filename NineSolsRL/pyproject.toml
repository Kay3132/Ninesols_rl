[project]
name = "ninesols-rl"
version = "0.1.0"
description = "Nine Sols 強化學習環境（透過 BepInEx mod + TCP 橋接）"
requires-python = ">=3.10"
dependencies = [
    "gymnasium>=0.29",
    "numpy>=1.24",
    "stable-baselines3>=2.3",
]

# 這是應用程式專案，不是要發佈的套件，uv 不需要 build 它
[tool.uv]
package = false
